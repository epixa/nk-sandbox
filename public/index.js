'use strict';

// ------------------------------------------------
// This chunk of code would be provided generated by the server in the
// index.html. The imports would be replaced by JS files that store the
// default export on window to be used here. Webpack will not be involved.

import dashboard from './applications/dashboard';
import discover from './applications/discover';
import visualize from './applications/visualize';

window.__kbn__ = {
  applications: [
    {
      name: 'dashboard',
      order: 2,
      component: dashboard
    },
    {
      name: 'discover',
      order: 0,
      component: discover
    },
    {
      name: 'visualize',
      order: 1,
      component: visualize
    },
  ]
};
// ------------------------------------------------


import { render } from 'react-dom';
import createCore from './core';

render(
  createCore({
    applications: window.__kbn__.applications
  }),
  document.getElementById('root')
);
